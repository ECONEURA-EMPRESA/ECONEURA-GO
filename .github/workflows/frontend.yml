name: Frontend CI

on:
  push:
    paths:
      - 'packages/frontend/**'
      - 'packages/shared/**'
      - 'packages/config/**'
      - 'package-lock.json'
      - '.github/workflows/frontend.yml'
  pull_request:
    paths:
      - 'packages/frontend/**'
      - 'packages/shared/**'
      - 'packages/config/**'
      - 'package-lock.json'
      - '.github/workflows/frontend.yml'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-test:
    name: Build & Test Frontend
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Lint Frontend Code
        run: npx turbo run lint --filter=@econeura/web...

      - name: Type Check Frontend
        run: npx turbo run type-check --filter=@econeura/web...

      - name: Build Frontend
        run: npx turbo run build --filter=@econeura/web...

      - name: Test Frontend
        run: npx turbo run test --filter=@econeura/web...
