name: Deploy to Azure

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - develop

env:
  NODE_VERSION: '20.x'
  AZURE_WEBAPP_NAME_BACKEND: econeura-backend-production
  AZURE_WEBAPP_PACKAGE_PATH_BACKEND: 'packages/backend'

permissions:
  id-token: write
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  deploy-backend:
    name: Deploy Backend
    runs-on: ubuntu-latest
    environment: production
    timeout-minutes: 30
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install Turbo
        run: npm install -g turbo

      - name: Prune Backend
        run: turbo prune --scope=@econeura/backend

      - name: Install Dependencies (Pruned)
        working-directory: out
        run: npm install

      - name: Build Backend
        working-directory: out
        run: npx turbo run build --filter=@econeura/backend...

      - name: Deploy to App Service
        uses: azure/webapps-deploy@v2
        with:
          app-name: ${{ env.AZURE_WEBAPP_NAME_BACKEND }}
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE_BACKEND }}
          package: out/packages/backend

  deploy-frontend:
    name: Deploy Frontend
    runs-on: ubuntu-latest
    environment: production
    timeout-minutes: 30
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build Frontend
        run: npx turbo run build --filter=@econeura/webapp...

      - name: Deploy to Static Web Apps
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: "upload"
          app_location: "packages/frontend"
          skip_app_build: true
